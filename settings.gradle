pluginManagement {
    resolutionStrategy {
        eachPlugin {
            if (requested.id.id == "kotlin-multiplatform") {
                useModule("org.jetbrains.kotlin:kotlin-gradle-plugin:${requested.version}")
            }
        }
    }

    repositories {
        maven {
            url 'https://teamcity.jetbrains.com/guestAuth/app/rest/builds/buildType:(id:Kotlin_dev_Compiler),number:1.2.70-dev-1606,branch:default:any/artifacts/content/maven/'
        }

        mavenCentral()

        maven {
            url 'https://plugins.gradle.org/m2/'
        }
    }
}
rootProject.name = 'mpp-test-002'

